# Frontend demo mode is the default. Set to true only when the Spring API is running.
VITE_ENABLE_API=false

# Google OAuth client identifier. A backend callback/token exchange is still required.
VITE_GOOGLE_CLIENT_ID=
